const AWA = ({ testSections }: { testSections: TestRecord[] }) => {
  const selectedTestSection = testSections?.[0]
  console.log({ selectedTestSection })
  return <div>{/*  TODO: use textfield component here */}</div>
}

export default AWA
